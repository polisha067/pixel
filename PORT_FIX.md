# 🔧 Исправление ошибки порта (WinError 10013)

## Проблема
```
ERROR: [WinError 10013] Сделана попытка доступа к сокету методом, запрещенным правами доступа
```

Это означает, что **порт 8000 уже занят** другим процессом.

## Решения:

### Решение 1: Использовать другой порт (рекомендуется)

Измените порт при запуске:

```bash
uvicorn backend.main:app --reload --host 127.0.0.1 --port 8001
```

Или установите в `.env` файле:
```env
PORT=8001
HOST=127.0.0.1
```

**Важно:** После изменения порта обновите `frontend/js/api.js`:
```javascript
const API_BASE_URL = 'http://localhost:8001';  // Измените на новый порт
```

### Решение 2: Освободить порт 8000

Найдите процесс, занимающий порт:
```bash
netstat -ano | findstr :8000
```

Завершите процесс (замените PID на номер из команды выше):
```bash
taskkill /PID <номер_процесса> /F
```

### Решение 3: Использовать другой порт в config.py

Я уже изменил порт по умолчанию на 8001 в `backend/config.py`.

Теперь запускайте:
```bash
uvicorn backend.main:app --reload --host 127.0.0.1 --port 8001
```

И обновите `frontend/js/api.js`:
```javascript
const API_BASE_URL = 'http://localhost:8001';
```

## ✅ Быстрое исправление:

1. **Измените порт в `frontend/js/api.js`:**
   ```javascript
   const API_BASE_URL = 'http://localhost:8001';
   ```

2. **Запустите с новым портом:**
   ```bash
   uvicorn backend.main:app --reload --host 127.0.0.1 --port 8001
   ```

3. **Проверьте работу:**
   - http://localhost:8001
   - http://localhost:8001/docs

---

**Готово! Порт 8001 должен работать! 🚀**

