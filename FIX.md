# 🔧 Исправление проблем с запуском

## Проблема 1: ModuleNotFoundError: No module named 'backend'

**Причина:** Запускаете из папки `backend/` вместо корня проекта.

**Решение:** Запускайте из корня проекта (папка `pixel/`):

```bash
# ✅ ПРАВИЛЬНО (из корня проекта):
uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000

# ❌ НЕПРАВИЛЬНО (из папки backend):
cd backend
python main.py  # Не работает!
```

## Проблема 2: ImportError: email-validator is not installed

**Причина:** Не установлен модуль `email-validator` для валидации email.

**Решение:** Установите зависимости:

```bash
pip install email-validator
pip install 'pydantic[email]'
```

Или обновите requirements.txt и установите всё:
```bash
pip install -r requirements.txt
```

## ✅ Правильная последовательность запуска:

### 1. Установите зависимости
```bash
# Убедитесь, что вы в корне проекта (папка pixel/)
pip install -r requirements.txt
```

### 2. Запустите backend (из корня проекта!)
```bash
uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```

Вы должны увидеть:
```
🔧 Мок-режим включен: проверка API ключей пропущена
✅ Все настройки загружены успешно!
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### 3. Откройте frontend
Откройте `frontend/index.html` в браузере.

## 🔍 Проверка работы:

1. Откройте http://localhost:8000 - должно показать JSON с сообщением
2. Откройте http://localhost:8000/docs - документация API
3. Откройте http://localhost:8000/health - проверка здоровья

## ⚠️ Если всё ещё не работает:

1. **Проверьте, что вы в корне проекта:**
   ```bash
   # Должны видеть файлы: requirements.txt, README.md, backend/, frontend/
   ls  # Linux/Mac
   dir  # Windows
   ```

2. **Проверьте виртуальное окружение:**
   ```bash
   # Активировано ли?
   which python  # Linux/Mac
   where python  # Windows
   ```

3. **Проверьте установленные пакеты:**
   ```bash
   pip list | grep fastapi
   pip list | grep uvicorn
   pip list | grep email-validator
   ```

4. **Попробуйте запустить через Python модуль:**
   ```bash
   python -m uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
   ```

---

**Если проблема остаётся, пришлите полный текст ошибки!**

