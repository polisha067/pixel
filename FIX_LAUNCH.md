# 🔧 Исправление проблем с запуском

## Проблема: ModuleNotFoundError

Если вы видите ошибку:
```
ModuleNotFoundError: No module named 'dotenv'
```

Это значит, что не установлены зависимости проекта.

## ✅ Решение:

### Шаг 1: Убедитесь, что вы в корне проекта

```bash
# Должны быть в папке pixel
cd C:\Users\lenovo\OneDrive\Рабочий стол\маи\хакатон псб\pixel
```

### Шаг 2: Создайте виртуальное окружение (если еще не создано)

```bash
python -m venv venv
```

### Шаг 3: Активируйте виртуальное окружение

**Windows (PowerShell):**
```bash
venv\Scripts\Activate.ps1
```

**Windows (CMD):**
```bash
venv\Scripts\activate.bat
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

После активации вы увидите `(venv)` в начале строки терминала.

### Шаг 4: Установите зависимости

```bash
pip install -r requirements.txt
```

Это установит все необходимые пакеты:
- fastapi
- uvicorn
- python-dotenv
- requests
- sqlalchemy
- pydantic
- и другие

### Шаг 5: Проверьте установку

```bash
python -c "import fastapi; print('FastAPI установлен!')"
```

### Шаг 6: Запустите сервер

```bash
cd backend
python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Или из корня проекта:

```bash
python -m uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```

## 🐛 Другие возможные проблемы:

### Проблема: "python: command not found"

**Решение:** Используйте `python3` вместо `python`:
```bash
python3 -m venv venv
python3 -m pip install -r requirements.txt
```

### Проблема: "Permission denied" при установке

**Решение:** Используйте `--user`:
```bash
pip install --user -r requirements.txt
```

### Проблема: Порт 8000 уже занят

**Решение:** Используйте другой порт:
```bash
uvicorn backend.main:app --reload --host 0.0.0.0 --port 8001
```

И измените в `frontend/js/api.js`:
```javascript
const API_BASE_URL = 'http://localhost:8001';
```

### Проблема: Ошибки импорта (ImportError)

**Решение:** Убедитесь, что запускаете из корня проекта:
```bash
# Правильно:
python -m uvicorn backend.main:app --reload

# Неправильно (если вы в папке backend):
python -m uvicorn main:app --reload
```

## 📝 Полная последовательность команд:

```bash
# 1. Перейти в корень проекта
cd C:\Users\lenovo\OneDrive\Рабочий стол\маи\хакатон псб\pixel

# 2. Создать виртуальное окружение
python -m venv venv

# 3. Активировать (Windows)
venv\Scripts\activate

# 4. Установить зависимости
pip install -r requirements.txt

# 5. Запустить сервер
python -m uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```

## ✅ Проверка успешного запуска:

Если все работает, вы увидите:
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process
INFO:     Started server process
INFO:     Waiting for application startup.
🔧 Мок-режим включен: проверка API ключей пропущена
✅ Все настройки загружены успешно!
✅ База данных инициализирована!
INFO:     Application startup complete.
```

Затем откройте в браузере:
- http://localhost:8000 - API
- http://localhost:8000/docs - Документация API

---

**Если проблема не решена, пришлите полный текст ошибки!**

