# 🧪 Тестовый режим (без Yandex API)

## Как запустить проект в тестовом режиме

Проект может работать **без реального Yandex API** для тестирования интерфейса и логики.

### Шаг 1: Убедитесь, что мок-режим включен

В файле `backend/config.py` установлено:
```python
USE_MOCK_AI: bool = os.getenv("USE_MOCK_AI", "True").lower() == "true"
```

По умолчанию мок-режим **включен** (True), так что ничего менять не нужно!

### Шаг 2: Установите зависимости

```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\Scripts\activate

# Активировать (Linux/Mac)
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### Шаг 3: Запустите backend

```bash
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Вы увидите сообщение:
```
🔧 Мок-режим включен: проверка API ключей пропущена
✅ Все настройки загружены успешно!
```

### Шаг 4: Откройте frontend

Откройте `frontend/index.html` в браузере.

Или используйте HTTP сервер:
```bash
# Из корня проекта
python -m http.server 8080
```

Затем откройте http://localhost:8080

## 🎯 Что работает в мок-режиме

✅ **Классификация писем** - определяет категорию по ключевым словам:
- "кредит" → credit
- "страхование", "осаго", "каско" → insurance
- "ипотека" → mortgage
- и т.д.

✅ **Генерация ответов** - возвращает готовые шаблоны ответов для каждой категории

✅ **Чат редактирования** - улучшает ответы на основе просьб сотрудника

✅ **Вся логика приложения** - регистрация, маршрутизация, статусы, статистика

## 📝 Примеры тестовых запросов

### Для категории "credit":
- "Хочу оформить кредит на машину"
- "Мне нужен кредит"
- "Как получить кредит?"

### Для категории "insurance":
- "Хочу оформить ОСАГО"
- "Нужна страховка на машину"
- "Как оформить КАСКО?"

### Для категории "mortgage":
- "Хочу взять ипотеку"
- "Ипотечный кредит"
- "Оформление ипотеки"

## 🔄 Переключение на реальный API

Когда будете готовы использовать реальный Yandex API:

1. Получите API ключ и Folder ID (см. START.md)
2. Создайте файл `.env`:
   ```env
   USE_MOCK_AI=False
   YANDEX_API_KEY=ваш_ключ
   YANDEX_FOLDER_ID=ваш_folder_id
   ```
3. Перезапустите backend

## ⚠️ Ограничения мок-режима

- Ответы генерируются по шаблонам, а не нейросетью
- Классификация работает по ключевым словам (может быть неточной)
- Чат редактирования работает упрощенно

Но для тестирования интерфейса и логики этого достаточно!

---

**Удачи в тестировании! 🚀**

