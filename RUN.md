# 🚀 Правильный способ запуска проекта

## ⚠️ Важно: Запускайте из корня проекта!

Проект должен запускаться из корневой папки `pixel/`, а не из папки `backend/`.

## Шаг 1: Установите зависимости

```bash
# Убедитесь, что вы в корне проекта (папка pixel/)
pip install -r requirements.txt
```

Если возникают ошибки, установите отдельно:
```bash
pip install email-validator
pip install 'pydantic[email]'
```

## Шаг 2: Запустите backend

**Из корня проекта** (папка `pixel/`):

```bash
uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```

Или через Python модуль:
```bash
python -m uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```

## Шаг 3: Откройте frontend

Откройте `frontend/index.html` в браузере.

## ❌ Неправильные способы запуска:

```bash
# НЕ ТАК:
cd backend
python main.py  # ❌ Не работает из-за импортов

# НЕ ТАК:
cd backend
uvicorn main:app  # ❌ Не работает из-за импортов
```

## ✅ Правильный способ:

```bash
# Из корня проекта:
uvicorn backend.main:app --reload
```

## 🔍 Проверка работы:

После запуска откройте в браузере:
- http://localhost:8000 - API
- http://localhost:8000/docs - Документация API
- http://localhost:8000/health - Проверка здоровья

---

**Если всё равно не работает, проверьте:**
1. Установлены ли все зависимости: `pip list`
2. Активировано ли виртуальное окружение
3. Запускаете ли из корня проекта

