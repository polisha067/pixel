# 🚀 Инструкция по запуску проекта

## Шаг 1: Установка зависимостей

```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\Scripts\activate

# Активировать (Linux/Mac)
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

## Шаг 2: Настройка Yandex Cloud AI

1. Зарегистрируйтесь на https://cloud.yandex.ru/
2. Создайте каталог
3. Получите API ключ:
   - Перейдите в IAM → Сервисные аккаунты
   - Создайте сервисный аккаунт
   - Создайте API ключ для этого аккаунта
4. Найдите Folder ID:
   - В URL каталога (после `/folders/`)
   - Или в настройках каталога

## Шаг 3: Создание файла .env

Скопируйте `.env.example` в `.env` и заполните:

```env
YANDEX_API_KEY=ваш_api_ключ
YANDEX_FOLDER_ID=ваш_folder_id
```

## Шаг 4: Запуск backend

```bash
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Сервер запустится на http://localhost:8000

Документация API: http://localhost:8000/docs

## Шаг 5: Запуск frontend

Откройте `frontend/index.html` в браузере.

Или используйте простой HTTP сервер:

```bash
# Из корня проекта
python -m http.server 8080
```

Затем откройте http://localhost:8080

## Шаг 6: Тестирование

1. Откройте http://localhost:8080
2. Выберите "Я пользователь"
3. Зарегистрируйтесь
4. Отправьте тестовое обращение (например: "Хочу оформить кредит на машину")
5. Выберите "Я сотрудник"
6. Зарегистрируйтесь как сотрудник с категорией "credit"
7. Откройте назначенное письмо
8. Попробуйте отредактировать ответ через чат
9. Отправьте финальный ответ

## ⚠️ Важно

- Убедитесь, что backend запущен перед использованием frontend
- Если возникают ошибки CORS, проверьте настройки в `backend/main.py`
- API ключи должны быть в файле `.env` (не коммитьте его в Git!)

## 📝 Структура проекта

- `backend/` - FastAPI приложение
- `frontend/` - HTML интерфейс
- `.env` - Настройки (создайте из .env.example)

## 🐛 Решение проблем

### Ошибка: "YANDEX_API_KEY не установлен"
- Проверьте, что файл `.env` существует и содержит правильные ключи

### Ошибка: "CORS error"
- Убедитесь, что backend запущен на порту 8000
- Проверьте настройки CORS в `backend/main.py`

### Ошибка: "Не найдено ни одного сотрудника для категории"
- Зарегистрируйте сотрудника с нужной категорией перед отправкой письма

---

**Удачи! 🚀**

